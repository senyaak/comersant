describe('@ValidateActivePlayer', () => {
  it.todo('invokes the original method when the player id matches the current player');
  it.todo('throws when the supplied player id is not the active one');
  it.todo('forwards remaining arguments to the wrapped method unchanged');
});

describe('@RequireGameState', () => {
  it.todo('invokes the original method when stateManager is in the required state');
  it.todo('throws when stateManager is in any other state');
  it.todo('error message names both the required and the actual state');
});
