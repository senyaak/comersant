describe('computeMove', () => {
  it.todo('returns a single PLAYER_MOVED effect with the given playerIndex and step count');
});

describe('computeMoveToPosition', () => {
  it.todo('returns a single PLAYER_MOVED_TO_POSITION effect with the supplied target');
});

describe('computeMoveToTaxService', () => {
  it.todo('finds the TaxServiceCell index in the flat board and emits PLAYER_MOVED_TO_POSITION');
});

describe('computeMoveToCellName', () => {
  it.todo('resolves the cell name via Board.getTargetPosition and emits PLAYER_MOVED_TO_POSITION');
});
